/**
 * Custom Better Auth APIError codes raised by Sokosumi hooks. Shared so the
 * core auth instance (which throws them) and the web client components
 * (which match on them) agree on the exact string.
 */
export const ORGANIZATION_HAS_ADDITIONAL_MEMBERS_ERROR_CODE =
  "ORGANIZATION_HAS_ADDITIONAL_MEMBERS";
