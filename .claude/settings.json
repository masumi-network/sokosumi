{
  "enabledPlugins": {
    "typescript-lsp@claude-plugins-official": true
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "[ \"$CLAUDE_CODE_REMOTE\" = true ] || exit 0; cd \"$CLAUDE_PROJECT_DIR\" && bash scripts/cloud-agent-db/ensure-pnpm.sh install && pnpm prisma:generate",
            "timeout": 600
          }
        ]
      }
    ]
  }
}
