name: Apple
permissions:
  contents: read

on:
  workflow_dispatch:
  pull_request:
  # Lint and test on main after Apple merges so the Mint cache is saved on
  # the default branch. PR caches are scoped to refs/pull/N/merge and cannot
  # be restored by sibling PRs.
  push:
    branches:
      - main
    paths:
      - "apps/apple/**"
      - ".github/workflows/apple.yml"

concurrency:
  # A new push to the same PR cancels the queued or running run for the old
  # commit. Runs on main are never cancelled so the cache saves complete.
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  changes:
    name: Detect Apple changes
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      apple: ${{ steps.filter.outputs.apple }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Detect Apple-relevant changes
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            apple:
              - "apps/apple/**"
              - ".github/workflows/apple.yml"

  # CI signing note: the project carries a developer team for local runs, but
  # CI holds no Apple certificates, so xcodebuild overrides it with ad-hoc
  # signing (`DEVELOPMENT_TEAM=` `CODE_SIGN_IDENTITY=-`, see SOK-973).
  # `-skipPackagePluginValidation`: no interactive user to trust the
  # OpenAPIGenerator plugin in CI.
  #
  # There is no separate build job: `xcodebuild test` builds the same scheme,
  # configuration and destination before testing.
  test:
    name: Xcode test
    needs: changes
    # Same gate as `lint`: PR, dispatch, or path-filtered `push` to main.
    #
    # One action on the `Sokosumi` *workspace* scheme covers the app target
    # and all five package suites -- the workspace is what makes the package
    # tests reachable from the scheme, so do not add per-package jobs. This is
    # the same command `apps/apple/README.md` tells a human to run.
    #
    # Xcode Cloud builds releases only; it never reports on a pull request, so
    # this job is the Apple test gate. See
    # `apps/apple/docs/xcode-cloud-required-check.md`.
    if: needs.changes.outputs.apple == 'true' || github.event_name == 'workflow_dispatch' || github.event_name == 'push'
    runs-on: xcode-27
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Select Xcode 27 toolchain
        # Image default is Xcode 27; Xcode.app is the documented alias.
        run: sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Show Xcode version
        run: xcodebuild -version && swift --version

      - name: Test Sokosumi workspace scheme
        working-directory: apps/apple
        # Coverage stays off: ably-cocoa's C target (AblyDeltaCodec) cannot
        # link the profile runtime, so gathering coverage fails the test build
        # (SOK-976). Nothing consumes coverage today. Restore later by
        # excluding AblyDeltaCodec, not by re-enabling coverage here.
        run: xcodebuild -workspace Sokosumi.xcworkspace -scheme Sokosumi -configuration Debug -destination 'platform=macOS,arch=arm64' -skipPackagePluginValidation DEVELOPMENT_TEAM= CODE_SIGN_IDENTITY=- test -enableCodeCoverage NO -resultBundlePath "$RUNNER_TEMP/Sokosumi.xcresult"

      - name: Report test failures
        if: failure()
        run: |
          if [ -d "$RUNNER_TEMP/Sokosumi.xcresult" ]; then
            xcrun xcresulttool get test-results summary --path "$RUNNER_TEMP/Sokosumi.xcresult"
          fi

      - name: Preserve failed test results
        if: failure()
        uses: actions/upload-artifact@v7.0.1
        with:
          name: Sokosumi-xcresult
          path: ${{ runner.temp }}/Sokosumi.xcresult
          retention-days: 3
          if-no-files-found: ignore

  # Release is the one configuration nothing else compiles. `Xcode test` builds
  # Debug; Xcode Cloud builds Release but only when a release is cut, so a
  # whole-module-optimization or dead-stripping break would surface on the
  # archive that is meant to ship. This job moves that discovery to the merge
  # that caused it.
  #
  # Main pushes only -- no `needs: changes`, because `on.push.paths` above
  # already restricts main pushes to Apple changes, and Apple pull requests
  # already pay for a full `xcodebuild test`. Not a required status check: it
  # never runs on a pull request, so it has nothing to report there.
  release-build:
    name: Xcode release build
    if: github.event_name == 'push'
    runs-on: xcode-27
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Select Xcode 27 toolchain
        # Image default is Xcode 27; Xcode.app is the documented alias.
        run: sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Show Xcode version
        run: xcodebuild -version && swift --version

      - name: Build Sokosumi for Release
        working-directory: apps/apple
        # `build`, not `archive`: CI holds no Apple certificates, and Xcode
        # Cloud performs the real signed archive. The point here is the
        # compile, not the package.
        run: xcodebuild -workspace Sokosumi.xcworkspace -scheme Sokosumi -configuration Release -destination 'platform=macOS,arch=arm64' -skipPackagePluginValidation DEVELOPMENT_TEAM= CODE_SIGN_IDENTITY=- build

  lint:
    name: Swift lint and format
    needs: changes
    # PR, dispatch, or path-filtered `push` to main so `actions/cache` saves
    # ~/.mint on the default branch. `Swift lint and format` is a required
    # status check on `main`.
    if: needs.changes.outputs.apple == 'true' || github.event_name == 'workflow_dispatch' || github.event_name == 'push'
    runs-on: xcode-27
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Select Xcode 27 toolchain
        # Image default is Xcode 27; Xcode.app is the documented alias.
        run: sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Show Xcode version
        run: xcodebuild -version && swift --version

      - name: Install Mint
        run: brew install mint

      - name: Cache Mint packages
        uses: actions/cache@v6
        with:
          path: ~/.mint
          key: ${{ runner.os }}-mint-${{ hashFiles('apps/apple/Mintfile') }}

      - name: Bootstrap pinned Swift tools
        working-directory: apps/apple
        run: mint bootstrap

      - name: Check Swift formatting
        working-directory: apps/apple
        run: mint run swiftformat --lint .

      - name: Lint Swift
        working-directory: apps/apple
        run: mint run swiftlint lint --strict
