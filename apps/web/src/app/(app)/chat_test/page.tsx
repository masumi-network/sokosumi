/**
 * Chat UI is rendered by the layout; this page keeps the route active.
 * Not linked in the sidebar; use /chat_test to test mobile keyboard UX.
 */
export default function ChatTestPage() {
  return null;
}
