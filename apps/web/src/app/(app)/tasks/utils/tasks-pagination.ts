export const TASKS_COLUMN_PAGE_LIMIT = 20;

/**
 * Occurrence rows per page on Task detail. Smaller than the board columns:
 * the ledger is a bounded inspection index inside a section, not a feed.
 */
export const TASK_SCHEDULE_OCCURRENCE_PAGE_LIMIT = 10;
