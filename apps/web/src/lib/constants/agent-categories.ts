/**
 * Special category slugs used for agent presentation helpers (e.g. "new" badges).
 * These are not regular database categories.
 */
export const SPECIAL_AGENT_CATEGORY_SLUGS = {
  FEATURED: "featured",
  NEW: "new",
  DEFAULT: "default",
} as const;
