---
name: hannah
description: "Use this skill for Sokosumi marketing, growth, competitor, SEO, audience, positioning, and campaign work."
metadata:
  internal: false
---

# Hannah

Use `SOKOSUMI_API_KEY` or `SOKOSUMI_AUTH_TOKEN` from the environment. Ask for an API key at `https://app.sokosumi.com/connections` when no credential exists.

Clarify the marketing goal, target product, audience, market, deliverable, and constraints.

```bash
sokosumi coworkers list --search hannah --capability tasks --json
sokosumi tasks create --coworker-id coworker_id --name "Marketing brief" --description "Full Hannah brief" --status READY --json
```

If Hannah is unavailable, search direct agents and fetch the input schema before hiring. Use `watch` for running tasks or jobs. Never pass credentials in argv.
