# Remaining CLI TUI

Tasks 1–7 are spent (config, hosted OAuth, selector TUI, Core transport, headless commands, resource views, npm updater). Updater source/tests are deleted. Source of truth: `SPEC.md`.

Live remainder is SPEC `T27`: Ink TUI vs the external v1 bundle. Keep dark chrome, arrows + Enter, Esc back, q quit, live Core data. No HTML runtime, no prototype fixtures. Open SPEC §T rows (`T8`, `T13`, `T27`, …) stay in SPEC.
